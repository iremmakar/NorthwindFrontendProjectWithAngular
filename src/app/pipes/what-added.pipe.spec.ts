import { WhatAddedPipe } from './what-added.pipe';

describe('WhatAddedPipe', () => {
  it('create an instance', () => {
    const pipe = new WhatAddedPipe();
    expect(pipe).toBeTruthy();
  });
});
